fastapi
uvicorn
requests
audioop-lts; python_version>="3.13"

# Note: streamlit is intentionally not pinned here. The Streamlit dashboard
# (dashboard/app.py) is a fallback only; the primary frontend is the Next.js
# app under frontend/. To run the Streamlit fallback locally:
#   pip install streamlit>=1.30
